[core]
	excludesfile = /home/hugo/.gitignore_global
	editor = vim
[user]
	email = hugo@hornquist.se
	name = Hugo Hörnquist
[push]
	default = current
[alias]
	gud = diff HEAD
	list = log --all --oneline --decorate --graph
	llist = log --all --pretty=short --decorate --graph
[github]
	user = HugoNikanor

[url "https://aur.archlinux.org/"]
	insteadOf = "aur:"
[url "ssh+git://aur4.archlinux.org/"]
    pushInsteadOf = "aur:"

[url "https://github.com/"]
    insteadOf = "gh:"
# Enable these when I have set up an ssh key
# [url "git@github.com:"]
#     pushInsteadOf = "gh:"
