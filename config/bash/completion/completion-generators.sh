# Programs which generates their own completion on the fly.

cache_completion pandoc --bash-completion

# Fly is the Concourse CI <https://concourse-ci.org/> client command line.
cache_completion fly completion --shell bash

# Markdown CLI renderer
cache_completion glow completion bash
